# Security

Do not publish real Facebook exports or generated case outputs in this
repository.

If sensitive data is accidentally committed, rotate or revoke affected secrets,
remove the data from Git history, and treat the repository as compromised until
reviewed.
